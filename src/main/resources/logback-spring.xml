<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    
    <!-- 引用Spring Boot的默认配置 -->
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>
    <include resource="org/springframework/boot/logging/logback/console-appender.xml"/>
    
    <!-- 自定义审计日志Appender -->
    <appender name="AUDIT_LOG_APPENDER" class="com.bing.framework.util.AuditLogAppender">
        <!-- 可以在这里添加自定义属性 -->
    </appender>
    
    <!-- 常规文件日志Appender -->
    <appender name="FILE_APPENDER" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE:-logs/bing-framework.log}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- 按天回滚 -->
            <fileNamePattern>logs/bing-framework.%d{yyyy-MM-dd}.log</fileNamePattern>
            <!-- 保留30天的日志 -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- 审计日志记录器 -->
    <logger name="AUDIT_LOG" level="INFO" additivity="false">
        <appender-ref ref="AUDIT_LOG_APPENDER" />
        <appender-ref ref="FILE_APPENDER" />
    </logger>
    
    <!-- 根日志配置 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE_APPENDER" />
    </root>
    
</configuration>